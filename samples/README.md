# Samples

Representative saved `RunTrace` files:

- `01_happy_path.json` - vector database comparison using web search plus GitHub repository metadata.
- `02_graceful_failure.json` - nonexistent startup funding query that should stay low-confidence and avoid fabricated funding rounds.
- `03_tool_skip.json` - trivial arithmetic query where the planner can skip tools and synthesize directly.
