[project]
name = "bot"
version = "0.1.0"
description = "CLI research agent for grounded multi-source answers."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-genai",
    "groq",
    "httpx",
    "openai",
    "pydantic",
    "pydantic-settings",
    "pyyaml",
    "structlog",
    "tavily-python",
    "tenacity",
    "trafilatura",
    "typer",
]

[project.scripts]
bot = "bot.cli:app"
bot-eval = "bot.evals.runner:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
